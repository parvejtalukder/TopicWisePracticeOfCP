#include <bits/stdc++.h>
using namespace std;

void pht() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
}

int main() {
    pht();
    int tc;
    cin >> tc;
    while(tc--) {
        int n;
        cin >> n;
        int an = n;
        vector <int> arr(n);
        // map <int, int> ans;
        for(int i = 0; i < n; i++) {
            cin >> arr[i];
        }
        for(int i = 0; i < n ; i++) {
            
        }
        cout << n - an << "\n";
        // if ()
    }
    return 0;
}