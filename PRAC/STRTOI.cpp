#include <bits/stdc++.h>
using namespace std;

void pht() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
}

int main() {
    pht();
    string n = "30";
    int num = stoi(n);
    cout << num / 2 << endl;
    return 0;
}
