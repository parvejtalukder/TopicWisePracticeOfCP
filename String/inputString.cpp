#include <string.h>
#include <bits/stdc++.h>
using namespace std;

int main() {
    vector <int> myVec;
    myVec.capacity();
    char myName[40];
    cin.getline(myName, 40);
    int size = strlen(myName);
    cout << myName << endl << size << endl;
    int charSize;
    charSize = size;
    cout << charSize << endl;
    return 0;
}